BigQuery data exploration, querying, and optimization. Use when working with BigQuery data, writing BQ SQL queries, exploring dataset schemas, profiling tables, sampling data (top 10s, head), gener...