port: {{PORT}}
replicas:
  min: {{REPLICAS_MIN}}
  max: {{REPLICAS_MAX}}

# Vault configuration (populated in Phase 4 Q4e)
# vault_role: {{PROJECT_ID}}    # Vault role = project name (Kubernetes auth role in cps/projects)
