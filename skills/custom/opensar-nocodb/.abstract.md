Browse and manage database records via NocoDB MCP servers. Covers two bases -- OpenSAR (default, cloud_sample + opensar_enhanced) and Tax (opt-in, only when user mentions "tax"). Use when querying,...